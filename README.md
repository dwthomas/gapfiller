# OpprotunisticSonarScan

Install Anaconda
conda env create --name gapfiller --file=gapfiller_env.yml

sudo apt install libboost-all-dev cmake ninja-build build-essential libgdal-dev libgeographic-dev

pip install manifold3d trimesh pyvista antimeridian